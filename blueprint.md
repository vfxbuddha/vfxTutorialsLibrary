# VFX 튜토리얼 큐레이션 웹사이트 프로젝트

## 1. 프로젝트 개요

VFX(시각 효과)와 관련된 고품질의 튜토리얼 콘텐츠를 수집하고 분류하여 사용자에게 제공하는 웹사이트입니다. YouTube, Vimeo 등의 비디오 플랫폼과 전문 블로그의 아티클을 큐레이션하며, 사용자가 직접 작성한 튜토리얼을 게시할 수 있는 개인 블로그 기능도 포함합니다.

## 2. 디자인 및 기능

### 디자인 컨셉
- **테마:** 어둡고(Dark) 현대적인 테크(Tech) 중심의 미학
- **레이아웃:** 직관적인 탐색을 위한 상단 네비게이션 바, 카드 기반 콘텐츠 표시, 깔끔한 푸터
- **상호작용:** 부드러운 전환 효과와 호버 애니메이션

### 주요 기능
- **동적 콘텐츠 로딩:** `data.json` 파일에서 튜토리얼 데이터를 비동기적으로 로드하여 표시합니다. 관리자는 이 파일만 수정하여 콘텐츠를 쉽게 추가/변경할 수 있습니다.
- **SPA (Single Page Application) 탐색:** 페이지 새로고침 없이 부드럽게 섹션 간을 이동합니다.
- **대시보드형 홈 페이지:** 각 섹션(비디오, 아티클, 블로그)의 최신 게시글 3개를 요약하여 보여줍니다.
- **반응형 디자인:** 모바일, 태블릿, 데스크톱 등 모든 화면 크기를 지원합니다.

## 3. 기술 스택

- **프론트엔드:** HTML5, CSS3, Vanilla JavaScript (ES6+)
- **데이터 관리:** `data.json` 파일을 통한 정적 데이터 관리

## 4. 콘텐츠 관리 시스템 구현 계획 (현재 요청)

**목표:** 관리자가 HTML을 직접 수정하지 않고 `data.json` 파일을 통해 튜토리얼을 쉽게 추가/관리할 수 있는 시스템 구축.

1.  **`blueprint.md` 업데이트:** 콘텐츠 관리 방식을 `JSON`으로 변경하는 계획을 문서에 반영합니다. (완료)
2.  **`data.json` 파일 생성:**
    -   `videoTutorials`, `articleTutorials`, `blogPosts` 세 개의 키를 가진 JSON 객체를 생성합니다.
    -   각 키는 튜토리얼 객체의 배열을 값으로 가집니다.
    -   각 튜토리얼 객체는 `id`, `title`, `description`, `imageUrl`, `source`, `url` 등의 속성을 포함합니다.
3.  **`index.html` 구조 수정:**
    -   기존에 하드코딩되어 있던 `.card` 요소들을 모두 삭제합니다.
    -   JavaScript가 동적으로 카드를 삽입할 `.card-container`만 남겨둡니다.
4.  **`main.js` 로직 수정:**
    -   `DOMContentLoaded` 이벤트 발생 시, `fetch`를 이용해 `data.json` 파일을 불러옵니다.
    -   `renderCards` 함수를 구현하여, 가져온 데이터를 기반으로 HTML 카드 요소를 동적으로 생성하고 각 섹션의 `.card-container`에 삽입합니다.
    -   모든 카드가 렌더링된 후, 기존의 `populateHomePage` 함수를 호출하여 홈 화면을 채웁니다.
